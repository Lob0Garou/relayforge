import { render,screen } from '@testing-library/react'; import { describe,it,expect } from 'vitest'; import { StatusBadge } from '../src/components';
describe('StatusBadge',()=>{it('uses visible text in addition to color',()=>{render(<StatusBadge status="DeadLettered"/>);expect(screen.getByText('Dead letter')).toBeVisible();});it('uses endpoint-specific active and neutral inactive labels',()=>{const{rerender}=render(<StatusBadge status="Active"/>);expect(screen.getByText('Active')).toBeVisible();rerender(<StatusBadge status="Inactive"/>);expect(screen.getByText('Inactive')).toHaveClass('status-inactive');});});
