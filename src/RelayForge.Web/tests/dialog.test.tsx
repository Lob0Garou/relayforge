import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{describe,expect,it,vi}from'vitest';import{ReplayDialog}from'../src/app';
describe('ReplayDialog',()=>{it('focuses cancel, traps Tab, closes on Escape and restores trigger focus',async()=>{const user=userEvent.setup();const trigger=document.createElement('button');document.body.append(trigger);trigger.focus();const close=vi.fn();render(<ReplayDialog deliveryId="11111111-1111-1111-1111-111111111111" pending={false} onCancel={close} onConfirm={vi.fn()} returnFocus={trigger}/>);expect(screen.getByRole('button',{name:'Cancel'})).toHaveFocus();await user.tab({shift:true});expect(screen.getByRole('button',{name:'Confirm replay'})).toHaveFocus();await user.keyboard('{Escape}');expect(close).toHaveBeenCalledOnce();expect(trigger).toHaveFocus();trigger.remove()});it('disables confirmation while pending',()=>{render(<ReplayDialog deliveryId="11111111-1111-1111-1111-111111111111" pending onCancel={vi.fn()} onConfirm={vi.fn()}/>);expect(screen.getByRole('button',{name:'Confirm replay'})).toBeDisabled()})});
